<!DOCTYPE html>
<html lang="en">

<head>
  <style>
    body {
      font-family: Arial, Helvetica, sans-serif;
    }
  </style>
</head>

<body>
  <strong>Olá, {{ad.author.name}}</strong>
  <p>Voçê tem uma nova solicitação de compra ara o anuncio {{ad.title}}</p>
  <br>
  <strong>{{user.name}}({{user.email}}):</strong>
  <p>{{content}}</p>
</body>

</html>
